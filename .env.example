# Optional: Custom CORS proxy URL for git clone operations
# Falls back to cors.isomorphic-git.org if not set
VITE_CORS_PROXY_URL=https://repoguru-git-proxy.andy-rea.workers.dev

# GitHub App client ID for "Sign in with GitHub" OAuth flow
# Create a GitHub App at https://github.com/settings/apps/new
# Permissions: Contents (read), Metadata (read)
VITE_GITHUB_CLIENT_ID=
